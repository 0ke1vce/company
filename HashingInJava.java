package com.company;

public class HashingInJava {
    public static void main(String[] args) {
        /*
        Hashing is the technique to convert the range of key-value pairs to a range of indices.
         In hashing, we use hash functions to map keys to some values.
         */

        /*
        Collision:
         The hash function may map two key values to a single index. Such a situation is known as a collision.
Example : Let 1l=[22,42,64,71]
H(x) = keys%10
key 22 and 42 point to the same hashtable 2 hence we have to handle it.
         */
    }
}
